# Development Profile Configuration

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.school.student: DEBUG
    org.springframework.web: DEBUG
