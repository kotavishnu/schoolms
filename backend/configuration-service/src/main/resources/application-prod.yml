spring:
  datasource:
    hikari:
      maximum-pool-size: 50
  jpa:
    show-sql: false

logging:
  level:
    com.school.configuration: INFO
    org.springframework.web: WARN
    org.hibernate: WARN
