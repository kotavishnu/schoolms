spring:
  datasource:
    hikari:
      maximum-pool-size: 10
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.school.configuration: DEBUG
    org.springframework.web: DEBUG
    org.hibernate: DEBUG
